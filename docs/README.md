# multiplexor docs

These docs explain the v1 workflow:

- `usage.md`: how a primary agent calls `multiplexor`.
- `configuration.md`: provider config, scoring and state.
- `security.md`: permissions, credentials and operational risk.

The CLI is the product core. There is no MCP server in v1.
